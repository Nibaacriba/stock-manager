export type Product = {
  name: string;
  quantity: number;
  price: number;
  category: string; // mesa, cadeira, etc
  description: string;
  id: string;
  createdAt: string;
  updatedAt?: string;
};
